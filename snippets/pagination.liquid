{%- if paginate.pages > 1 -%}
  <div class="Pagination {% if tight %}Pagination--tight{% endif %} Text--subdued">
    <div class="Pagination__Nav">
      {%- if paginate.previous -%}
        <a class="newsList__pagination__nav__item" rel="prev" title="{{ 'general.pagination.previous_page' | t }}" href="{{ paginate.previous.url | remove: 'view=ajax' }}{{ hash }}">
          {%- render 'icon' with 'newsList-select-arrow-left' -%}
        </a>
      {%- endif -%}

      {%- for part in paginate.parts -%}
        {%- if part.is_link -%}
          <a href="{{ part.url | remove: 'view=ajax' }}{{ hash }}" class="newsList__pagination__nav__item Link Link--primary" title="{{ 'general.pagination.go_to_page' | t: page: part.title }}">{{ part.title }}</a>
        {%- else -%}
          <span class="newsList__pagination__nav__item {% if part.title == paginate.current_page %}newsList__active{% endif %}">{{ part.title }}</span>
        {%- endif -%}
      {%- endfor -%}

      {%- if paginate.next -%}
        <a class="newsList__pagination__nav__item" rel="next" title="{{ 'general.pagination.next_page' | t }}" href="{{ paginate.next.url | remove: 'view=ajax' }}{{ hash }}">
          {%- render 'icon' with 'newsList-select-arrow-right' -%}
        </a>
      {%- endif -%}
    </div>
  </div>
{%- endif -%}